Wallets
    Add transaction & verify wallets ✅
    npx playwright test e2e/wallets/wallet-add-transaction.spec.ts --headed
        For each transaction type (Split, Swing, Hold):
            Add transaction
            Verify wallets
    Delete transaction & verify wallets ✅
    npx playwright test e2e/wallets/wallet-delete-transaction.spec.ts --headed
        For each transaction type (Split, Swing, Hold):
            Add transaction
            Delete transaction
            Verify wallets
    Edit transaction & verify wallets
        Split
        Swing
        Hold
    Add same price transaction & verify wallets
        Split
        Swing
        Hold

    